import:
    - run_settings.yaml
    - cluster_environment.yaml
    - iterations_sensitivities_s.yaml

#
# MODEL SETTINGS OVERRIDE
#

override:
    group_fraction:
        capacity:
            supply_power_renewable: ['==', 0.5]
            supply_power_nuclear: ['==', 0.0]
